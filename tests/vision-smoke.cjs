const {chromium}=require(process.env.PLAYWRIGHT_PATH||'playwright');
(async()=>{const browser=await chromium.launch({channel:process.env.BROWSER_CHANNEL||'msedge',headless:true,args:['--use-fake-device-for-media-stream','--use-fake-ui-for-media-stream']});try{const page=await browser.newPage();const errors=[];page.on('pageerror',e=>errors.push(e.message));page.on('requestfailed',r=>console.log('Request failed:',r.url(),r.failure()?.errorText));await page.goto(process.env.BASE_URL||'http://127.0.0.1:62455/');await page.locator('#camera-button').click();await page.waitForFunction(()=>poniDiagnostics().cameraActive,null,{timeout:45000});await page.waitForTimeout(1000);console.log('PASS: real MediaPipe 1.0.1 Face Detector loaded and processed simulated video.',await page.locator('#vision-status').innerText());if(errors.length)throw Error(errors.join(';'));await page.locator('#camera-button').click();if(await page.evaluate(()=>poniDiagnostics().cameraActive))throw Error('Camera did not stop');}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
